var convertToBase7 = function(num) {
    return num.toString(7);
   
};
//In JavaScript, we can use num.toString(base) to convert a number to different bases. The base can be any integer between 2 and 36.